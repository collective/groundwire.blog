from zope.publisher.interfaces.browser import IDefaultBrowserLayer

class IGroundwireBlogLayer(IDefaultBrowserLayer):
    """
    Browser layer for blog customizations.
    This layer is not used anymore, but is included for backward compatibility.
    """